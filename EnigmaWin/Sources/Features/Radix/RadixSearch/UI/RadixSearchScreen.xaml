<UserControl x:Class="EnigmaWin.Sources.Features.Radix.RadixSearch.UI.RadixSearchScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:EnigmaWin.Sources.Features.Radix.RadixSearch.UI">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title row -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding LabelTitle}" FontSize="24" FontWeight="SemiBold"/>
            <Button Grid.Column="1" Content="?" ToolTip="{Binding TooltipHelp}" Click="OnHelpClicked"/>
        </Grid>

        <!-- Search row -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding LabelPartOfName}" VerticalAlignment="Center" MinWidth="120" Margin="0,0,8,0"/>
                <TextBox Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         MinWidth="200" Margin="0,0,8,0"/>
                <Button Content="{Binding LabelSearch}" Click="OnSearchClicked"/>
            </StackPanel>
        </Border>

        <!-- Results -->
        <Grid Grid.Row="2">
            <TextBlock Text="{Binding LabelNoResults}"
                       Margin="0,8,0,0"
                       Opacity="0.7"
                       Visibility="{Binding HasNoResults, Converter={StaticResource BoolToVisibility}}"/>

            <ScrollViewer Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibility}}"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto">
                <StackPanel>
                    <Border BorderThickness="0,0,0,1" Padding="4,0,4,6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="90"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding LabelColName}"     FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding LabelColDateTime}" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="2" Text="{Binding LabelColLocation}" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="3" Text="" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="4" Text="" FontWeight="SemiBold"/>
                        </Grid>
                    </Border>

                    <ItemsControl ItemsSource="{Binding SearchResults}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type ui:HoroscopeSearchRow}">
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="200"/>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="140"/>
                                        <ColumnDefinition Width="90"/>
                                        <ColumnDefinition Width="90"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Name}"
                                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="4,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding DisplayDateTime}"
                                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="4,0"/>
                                    <TextBlock Grid.Column="2" Text="{Binding DisplayLocation}"
                                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="4,0"/>
                                    <Button Grid.Column="3" Content="{Binding LabelSelect}"
                                            Tag="{Binding}" Click="OnSelectClicked" Margin="2,1"/>
                                    <Button Grid.Column="4" Content="{Binding LabelDelete}"
                                            Tag="{Binding}" Click="OnDeleteClicked" Margin="2,1"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
