<UserControl x:Class="EnigmaWin.Sources.Features.Radix.RadixInput.UI.RadixInputScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:EnigmaWin.Sources.Features.Radix.RadixInput.UI">

    <Border Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="{Binding LabelTitle}" FontSize="24" FontWeight="SemiBold"
                               Margin="0,0,0,12"/>

                    <Expander x:Name="AboutSection"
                              IsExpanded="True"
                              Expanded="OnSectionExpanded"
                              GotFocus="OnSectionGotFocus"
                              HorizontalAlignment="Stretch"
                              Margin="0,0,0,8">
                        <Expander.Header>
                            <TextBlock Text="{Binding LabelAboutChart}" FontSize="16" FontWeight="SemiBold"/>
                        </Expander.Header>
                        <ui:AboutSectionView/>
                    </Expander>

                    <Expander x:Name="LocationSection"
                              Expanded="OnSectionExpanded"
                              GotFocus="OnSectionGotFocus"
                              HorizontalAlignment="Stretch"
                              Margin="0,0,0,8">
                        <Expander.Header>
                            <TextBlock Text="{Binding LabelLocation}" FontSize="16" FontWeight="SemiBold"/>
                        </Expander.Header>
                        <ui:LocationSectionView/>
                    </Expander>

                    <Expander x:Name="DateTimeSection"
                              Expanded="OnSectionExpanded"
                              GotFocus="OnSectionGotFocus"
                              HorizontalAlignment="Stretch">
                        <Expander.Header>
                            <TextBlock Text="{Binding LabelDateAndTime}" FontSize="16" FontWeight="SemiBold"/>
                        </Expander.Header>
                        <ui:DateTimeSectionView/>
                    </Expander>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="?" ToolTip="{Binding TooltipHelp}" Click="OnHelpClicked" Margin="0,0,8,0"/>
                <Button Content="Clear" Click="OnClearClicked" Margin="0,0,8,0"/>
                <Button Content="{Binding LabelCalculate}"
                        Click="OnCalculateClicked"
                        IsEnabled="{Binding CanCalculate}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
