<Window x:Class="EnigmaWin.Sources.Features.Radix.RadixAnalysis.Parans.UI.ParansHelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title, RelativeSource={RelativeSource Self}}"
        Width="520" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <StackPanel Margin="20">
        <ItemsControl ItemsSource="{Binding HelpParagraphs}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,0,0,10"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Content="{Binding LabelClose}" HorizontalAlignment="Right" Margin="0,12,0,0"
                Click="OnClose" MinWidth="80"/>
    </StackPanel>
</Window>
