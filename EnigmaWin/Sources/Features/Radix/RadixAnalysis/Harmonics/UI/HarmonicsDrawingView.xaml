<UserControl x:Class="EnigmaWin.Sources.Features.Radix.RadixAnalysis.Harmonics.UI.HarmonicsDrawingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="clr-namespace:EnigmaWin.Sources.Features.Radix.RadixAnalysis.Harmonics.UI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="800" d:DesignHeight="600">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title bar with action buttons -->
        <Border Grid.Row="0" BorderThickness="0,0,0,1" Padding="0,0,0,8" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="{Binding LabelTitle}"
                           FontSize="20"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ToggleButton IsChecked="{Binding IsBlackWhite, Mode=OneWay}"
                                  Command="{Binding ToggleBlackWhiteCommand}"
                                  ToolTip="{Binding LabelBtnBlackWhite}"
                                  Margin="0,0,4,0">
                        <Image Source="/Resources/Assets/color.png" Width="16" Height="16"/>
                    </ToggleButton>
                    <Button Content="{Binding LabelBtnExport}"
                            Margin="0,0,4,0"
                            Click="OnExportClicked"/>
                    <Button ToolTip="{Binding TooltipFactsheet}" Content="&#x2139;"
                            Margin="0,0,4,0"
                            Click="OnFactsheetClicked"/>
                    <Button Content="?"
                            ToolTip="{Binding TooltipHelp}"
                            Click="OnHelpClicked"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Wheel canvas -->
        <ui:HarmonicsWheelCanvas Grid.Row="1"
                                  PlotData="{Binding PlotData}"
                                  Theme="{Binding Theme}"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"/>

    </Grid>
</UserControl>
